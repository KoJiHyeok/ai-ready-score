# v0.4.1 - Package Condensed Agent Instructions

This patch release includes the condensed `AGENTS.md` instructions in the npm package.

## Changes

- Added `AGENTS.md` to the npm package file allowlist.
- Ships the shorter future-agent instructions introduced after the v0.4.0 tag.
- Confirmed README remains current for npm usage, `--init`, `--min-score`, JSON, Markdown, and bilingual output.

## Validation

```bash
npm test
npm run check
npm pack --dry-run
```

## Publishing

This release is intended to be published to npm as `ai-ready-score@0.4.1`.

---

# v0.4.1 - 압축된 에이전트 지침 패키징

이번 패치 릴리스는 압축된 `AGENTS.md` 지침을 npm 패키지에 포함합니다.

## 변경 사항

- npm 패키지 파일 allowlist에 `AGENTS.md`를 추가했습니다.
- v0.4.0 태그 이후 도입된 더 짧은 향후 에이전트 지침을 패키지에 포함합니다.
- README가 npm 사용법, `--init`, `--min-score`, JSON, Markdown, bilingual 출력 기준으로 최신 상태인지 확인했습니다.

## 검증

```bash
npm test
npm run check
npm pack --dry-run
```

## 배포

이번 릴리스는 `ai-ready-score@0.4.1`로 npm에 배포하기 위한 릴리스입니다.
